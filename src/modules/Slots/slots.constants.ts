export const slotAppointmentStatus = [
  'available',
  'booked',
  'canceled',
  'expired',
];

export const slotStatus = {
  available: 'available',
  booked: 'booked',
  canceled: 'canceled',
  expired: 'expired',
} as const;

export const slotSearchableFields = ['date'];
